name: Release

on:
  push:
    branches:
      - main

jobs:
  release-please:
    permissions:
      pull-requests: write
    runs-on: ubuntu-latest
    steps:
      - uses: GoogleCloudPlatform/release-please-action@v2
        with:
          release-type: simple
